using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class monster_011_fly_level : MonoBehaviour
{
    public int My_Level = 0;

    public void LevelUp()
    {
        My_Level += 1;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
