using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class monster_012_pooter_state : MonoBehaviour
{

    public float health;
    public float attack;

    // Start is called before the first frame update
    void Start()
    {
        health = 8.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
